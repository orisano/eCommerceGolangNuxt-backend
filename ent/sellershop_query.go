// Code generated by entc, DO NOT EDIT.

package ent

import (
	"bongo/ent/predicate"
	"bongo/ent/sellerproduct"
	"bongo/ent/sellershop"
	"bongo/ent/shopcategory"
	"bongo/ent/user"
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SellerShopQuery is the builder for querying SellerShop entities.
type SellerShopQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.SellerShop
	// eager-loading edges.
	withUser            *UserQuery
	withAdmin           *UserQuery
	withGetShopCategory *ShopCategoryQuery
	withSellerProducts  *SellerProductQuery
	withFKs             bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SellerShopQuery builder.
func (ssq *SellerShopQuery) Where(ps ...predicate.SellerShop) *SellerShopQuery {
	ssq.predicates = append(ssq.predicates, ps...)
	return ssq
}

// Limit adds a limit step to the query.
func (ssq *SellerShopQuery) Limit(limit int) *SellerShopQuery {
	ssq.limit = &limit
	return ssq
}

// Offset adds an offset step to the query.
func (ssq *SellerShopQuery) Offset(offset int) *SellerShopQuery {
	ssq.offset = &offset
	return ssq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ssq *SellerShopQuery) Unique(unique bool) *SellerShopQuery {
	ssq.unique = &unique
	return ssq
}

// Order adds an order step to the query.
func (ssq *SellerShopQuery) Order(o ...OrderFunc) *SellerShopQuery {
	ssq.order = append(ssq.order, o...)
	return ssq
}

// QueryUser chains the current query on the "user" edge.
func (ssq *SellerShopQuery) QueryUser() *UserQuery {
	query := &UserQuery{config: ssq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ssq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ssq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sellershop.Table, sellershop.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sellershop.UserTable, sellershop.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ssq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAdmin chains the current query on the "admin" edge.
func (ssq *SellerShopQuery) QueryAdmin() *UserQuery {
	query := &UserQuery{config: ssq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ssq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ssq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sellershop.Table, sellershop.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sellershop.AdminTable, sellershop.AdminColumn),
		)
		fromU = sqlgraph.SetNeighbors(ssq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGetShopCategory chains the current query on the "get_shop_category" edge.
func (ssq *SellerShopQuery) QueryGetShopCategory() *ShopCategoryQuery {
	query := &ShopCategoryQuery{config: ssq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ssq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ssq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sellershop.Table, sellershop.FieldID, selector),
			sqlgraph.To(shopcategory.Table, shopcategory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sellershop.GetShopCategoryTable, sellershop.GetShopCategoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(ssq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySellerProducts chains the current query on the "seller_products" edge.
func (ssq *SellerShopQuery) QuerySellerProducts() *SellerProductQuery {
	query := &SellerProductQuery{config: ssq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ssq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ssq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sellershop.Table, sellershop.FieldID, selector),
			sqlgraph.To(sellerproduct.Table, sellerproduct.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, sellershop.SellerProductsTable, sellershop.SellerProductsColumn),
		)
		fromU = sqlgraph.SetNeighbors(ssq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SellerShop entity from the query.
// Returns a *NotFoundError when no SellerShop was found.
func (ssq *SellerShopQuery) First(ctx context.Context) (*SellerShop, error) {
	nodes, err := ssq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sellershop.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ssq *SellerShopQuery) FirstX(ctx context.Context) *SellerShop {
	node, err := ssq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SellerShop ID from the query.
// Returns a *NotFoundError when no SellerShop ID was found.
func (ssq *SellerShopQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ssq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sellershop.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ssq *SellerShopQuery) FirstIDX(ctx context.Context) int {
	id, err := ssq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SellerShop entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one SellerShop entity is not found.
// Returns a *NotFoundError when no SellerShop entities are found.
func (ssq *SellerShopQuery) Only(ctx context.Context) (*SellerShop, error) {
	nodes, err := ssq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sellershop.Label}
	default:
		return nil, &NotSingularError{sellershop.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ssq *SellerShopQuery) OnlyX(ctx context.Context) *SellerShop {
	node, err := ssq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SellerShop ID in the query.
// Returns a *NotSingularError when exactly one SellerShop ID is not found.
// Returns a *NotFoundError when no entities are found.
func (ssq *SellerShopQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ssq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = &NotSingularError{sellershop.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ssq *SellerShopQuery) OnlyIDX(ctx context.Context) int {
	id, err := ssq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SellerShops.
func (ssq *SellerShopQuery) All(ctx context.Context) ([]*SellerShop, error) {
	if err := ssq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ssq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ssq *SellerShopQuery) AllX(ctx context.Context) []*SellerShop {
	nodes, err := ssq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SellerShop IDs.
func (ssq *SellerShopQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := ssq.Select(sellershop.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ssq *SellerShopQuery) IDsX(ctx context.Context) []int {
	ids, err := ssq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ssq *SellerShopQuery) Count(ctx context.Context) (int, error) {
	if err := ssq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ssq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ssq *SellerShopQuery) CountX(ctx context.Context) int {
	count, err := ssq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ssq *SellerShopQuery) Exist(ctx context.Context) (bool, error) {
	if err := ssq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ssq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ssq *SellerShopQuery) ExistX(ctx context.Context) bool {
	exist, err := ssq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SellerShopQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ssq *SellerShopQuery) Clone() *SellerShopQuery {
	if ssq == nil {
		return nil
	}
	return &SellerShopQuery{
		config:              ssq.config,
		limit:               ssq.limit,
		offset:              ssq.offset,
		order:               append([]OrderFunc{}, ssq.order...),
		predicates:          append([]predicate.SellerShop{}, ssq.predicates...),
		withUser:            ssq.withUser.Clone(),
		withAdmin:           ssq.withAdmin.Clone(),
		withGetShopCategory: ssq.withGetShopCategory.Clone(),
		withSellerProducts:  ssq.withSellerProducts.Clone(),
		// clone intermediate query.
		sql:  ssq.sql.Clone(),
		path: ssq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ssq *SellerShopQuery) WithUser(opts ...func(*UserQuery)) *SellerShopQuery {
	query := &UserQuery{config: ssq.config}
	for _, opt := range opts {
		opt(query)
	}
	ssq.withUser = query
	return ssq
}

// WithAdmin tells the query-builder to eager-load the nodes that are connected to
// the "admin" edge. The optional arguments are used to configure the query builder of the edge.
func (ssq *SellerShopQuery) WithAdmin(opts ...func(*UserQuery)) *SellerShopQuery {
	query := &UserQuery{config: ssq.config}
	for _, opt := range opts {
		opt(query)
	}
	ssq.withAdmin = query
	return ssq
}

// WithGetShopCategory tells the query-builder to eager-load the nodes that are connected to
// the "get_shop_category" edge. The optional arguments are used to configure the query builder of the edge.
func (ssq *SellerShopQuery) WithGetShopCategory(opts ...func(*ShopCategoryQuery)) *SellerShopQuery {
	query := &ShopCategoryQuery{config: ssq.config}
	for _, opt := range opts {
		opt(query)
	}
	ssq.withGetShopCategory = query
	return ssq
}

// WithSellerProducts tells the query-builder to eager-load the nodes that are connected to
// the "seller_products" edge. The optional arguments are used to configure the query builder of the edge.
func (ssq *SellerShopQuery) WithSellerProducts(opts ...func(*SellerProductQuery)) *SellerShopQuery {
	query := &SellerProductQuery{config: ssq.config}
	for _, opt := range opts {
		opt(query)
	}
	ssq.withSellerProducts = query
	return ssq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SellerShop.Query().
//		GroupBy(sellershop.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (ssq *SellerShopQuery) GroupBy(field string, fields ...string) *SellerShopGroupBy {
	group := &SellerShopGroupBy{config: ssq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ssq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ssq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.SellerShop.Query().
//		Select(sellershop.FieldName).
//		Scan(ctx, &v)
//
func (ssq *SellerShopQuery) Select(fields ...string) *SellerShopSelect {
	ssq.fields = append(ssq.fields, fields...)
	return &SellerShopSelect{SellerShopQuery: ssq}
}

func (ssq *SellerShopQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ssq.fields {
		if !sellershop.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ssq.path != nil {
		prev, err := ssq.path(ctx)
		if err != nil {
			return err
		}
		ssq.sql = prev
	}
	return nil
}

func (ssq *SellerShopQuery) sqlAll(ctx context.Context) ([]*SellerShop, error) {
	var (
		nodes       = []*SellerShop{}
		withFKs     = ssq.withFKs
		_spec       = ssq.querySpec()
		loadedTypes = [4]bool{
			ssq.withUser != nil,
			ssq.withAdmin != nil,
			ssq.withGetShopCategory != nil,
			ssq.withSellerProducts != nil,
		}
	)
	if ssq.withUser != nil || ssq.withAdmin != nil || ssq.withGetShopCategory != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, sellershop.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &SellerShop{config: ssq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, ssq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := ssq.withUser; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*SellerShop)
		for i := range nodes {
			if nodes[i].user_seller_shops == nil {
				continue
			}
			fk := *nodes[i].user_seller_shops
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(user.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "user_seller_shops" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.User = n
			}
		}
	}

	if query := ssq.withAdmin; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*SellerShop)
		for i := range nodes {
			if nodes[i].user_approved_shops == nil {
				continue
			}
			fk := *nodes[i].user_approved_shops
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(user.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "user_approved_shops" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Admin = n
			}
		}
	}

	if query := ssq.withGetShopCategory; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*SellerShop)
		for i := range nodes {
			if nodes[i].shop_category_seller_shops == nil {
				continue
			}
			fk := *nodes[i].shop_category_seller_shops
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(shopcategory.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "shop_category_seller_shops" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.GetShopCategory = n
			}
		}
	}

	if query := ssq.withSellerProducts; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*SellerShop)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.SellerProducts = []*SellerProduct{}
		}
		query.withFKs = true
		query.Where(predicate.SellerProduct(func(s *sql.Selector) {
			s.Where(sql.InValues(sellershop.SellerProductsColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.seller_shop_seller_products
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "seller_shop_seller_products" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "seller_shop_seller_products" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.SellerProducts = append(node.Edges.SellerProducts, n)
		}
	}

	return nodes, nil
}

func (ssq *SellerShopQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ssq.querySpec()
	_spec.Node.Columns = ssq.fields
	if len(ssq.fields) > 0 {
		_spec.Unique = ssq.unique != nil && *ssq.unique
	}
	return sqlgraph.CountNodes(ctx, ssq.driver, _spec)
}

func (ssq *SellerShopQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ssq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ssq *SellerShopQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   sellershop.Table,
			Columns: sellershop.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: sellershop.FieldID,
			},
		},
		From:   ssq.sql,
		Unique: true,
	}
	if unique := ssq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ssq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sellershop.FieldID)
		for i := range fields {
			if fields[i] != sellershop.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ssq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ssq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ssq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ssq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ssq *SellerShopQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ssq.driver.Dialect())
	t1 := builder.Table(sellershop.Table)
	columns := ssq.fields
	if len(columns) == 0 {
		columns = sellershop.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ssq.sql != nil {
		selector = ssq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ssq.unique != nil && *ssq.unique {
		selector.Distinct()
	}
	for _, p := range ssq.predicates {
		p(selector)
	}
	for _, p := range ssq.order {
		p(selector)
	}
	if offset := ssq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ssq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SellerShopGroupBy is the group-by builder for SellerShop entities.
type SellerShopGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ssgb *SellerShopGroupBy) Aggregate(fns ...AggregateFunc) *SellerShopGroupBy {
	ssgb.fns = append(ssgb.fns, fns...)
	return ssgb
}

// Scan applies the group-by query and scans the result into the given value.
func (ssgb *SellerShopGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := ssgb.path(ctx)
	if err != nil {
		return err
	}
	ssgb.sql = query
	return ssgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := ssgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(ssgb.fields) > 1 {
		return nil, errors.New("ent: SellerShopGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := ssgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) StringsX(ctx context.Context) []string {
	v, err := ssgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = ssgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) StringX(ctx context.Context) string {
	v, err := ssgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(ssgb.fields) > 1 {
		return nil, errors.New("ent: SellerShopGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := ssgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) IntsX(ctx context.Context) []int {
	v, err := ssgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = ssgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) IntX(ctx context.Context) int {
	v, err := ssgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(ssgb.fields) > 1 {
		return nil, errors.New("ent: SellerShopGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := ssgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := ssgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = ssgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) Float64X(ctx context.Context) float64 {
	v, err := ssgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(ssgb.fields) > 1 {
		return nil, errors.New("ent: SellerShopGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := ssgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := ssgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ssgb *SellerShopGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = ssgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (ssgb *SellerShopGroupBy) BoolX(ctx context.Context) bool {
	v, err := ssgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (ssgb *SellerShopGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range ssgb.fields {
		if !sellershop.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := ssgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ssgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ssgb *SellerShopGroupBy) sqlQuery() *sql.Selector {
	selector := ssgb.sql.Select()
	aggregation := make([]string, 0, len(ssgb.fns))
	for _, fn := range ssgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(ssgb.fields)+len(ssgb.fns))
		for _, f := range ssgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(ssgb.fields...)...)
}

// SellerShopSelect is the builder for selecting fields of SellerShop entities.
type SellerShopSelect struct {
	*SellerShopQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (sss *SellerShopSelect) Scan(ctx context.Context, v interface{}) error {
	if err := sss.prepareQuery(ctx); err != nil {
		return err
	}
	sss.sql = sss.SellerShopQuery.sqlQuery(ctx)
	return sss.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (sss *SellerShopSelect) ScanX(ctx context.Context, v interface{}) {
	if err := sss.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) Strings(ctx context.Context) ([]string, error) {
	if len(sss.fields) > 1 {
		return nil, errors.New("ent: SellerShopSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := sss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (sss *SellerShopSelect) StringsX(ctx context.Context) []string {
	v, err := sss.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = sss.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (sss *SellerShopSelect) StringX(ctx context.Context) string {
	v, err := sss.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) Ints(ctx context.Context) ([]int, error) {
	if len(sss.fields) > 1 {
		return nil, errors.New("ent: SellerShopSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := sss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (sss *SellerShopSelect) IntsX(ctx context.Context) []int {
	v, err := sss.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = sss.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (sss *SellerShopSelect) IntX(ctx context.Context) int {
	v, err := sss.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(sss.fields) > 1 {
		return nil, errors.New("ent: SellerShopSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := sss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (sss *SellerShopSelect) Float64sX(ctx context.Context) []float64 {
	v, err := sss.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = sss.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (sss *SellerShopSelect) Float64X(ctx context.Context) float64 {
	v, err := sss.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(sss.fields) > 1 {
		return nil, errors.New("ent: SellerShopSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := sss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (sss *SellerShopSelect) BoolsX(ctx context.Context) []bool {
	v, err := sss.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (sss *SellerShopSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = sss.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{sellershop.Label}
	default:
		err = fmt.Errorf("ent: SellerShopSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (sss *SellerShopSelect) BoolX(ctx context.Context) bool {
	v, err := sss.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (sss *SellerShopSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := sss.sql.Query()
	if err := sss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
