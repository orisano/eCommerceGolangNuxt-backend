// Code generated by SQLBoiler 4.8.3 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SellerShopProduct is an object representing the database table.
type SellerShopProduct struct {
	ID              int64      `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt       null.Time  `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt       null.Time  `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	DeletedAt       null.Time  `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	SellerProductID null.Int64 `boil:"seller_product_id" json:"seller_product_id,omitempty" toml:"seller_product_id" yaml:"seller_product_id,omitempty"`
	SellerShopID    null.Int64 `boil:"seller_shop_id" json:"seller_shop_id,omitempty" toml:"seller_shop_id" yaml:"seller_shop_id,omitempty"`

	R *sellerShopProductR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L sellerShopProductL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SellerShopProductColumns = struct {
	ID              string
	CreatedAt       string
	UpdatedAt       string
	DeletedAt       string
	SellerProductID string
	SellerShopID    string
}{
	ID:              "id",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
	DeletedAt:       "deleted_at",
	SellerProductID: "seller_product_id",
	SellerShopID:    "seller_shop_id",
}

var SellerShopProductTableColumns = struct {
	ID              string
	CreatedAt       string
	UpdatedAt       string
	DeletedAt       string
	SellerProductID string
	SellerShopID    string
}{
	ID:              "seller_shop_products.id",
	CreatedAt:       "seller_shop_products.created_at",
	UpdatedAt:       "seller_shop_products.updated_at",
	DeletedAt:       "seller_shop_products.deleted_at",
	SellerProductID: "seller_shop_products.seller_product_id",
	SellerShopID:    "seller_shop_products.seller_shop_id",
}

// Generated where

var SellerShopProductWhere = struct {
	ID              whereHelperint64
	CreatedAt       whereHelpernull_Time
	UpdatedAt       whereHelpernull_Time
	DeletedAt       whereHelpernull_Time
	SellerProductID whereHelpernull_Int64
	SellerShopID    whereHelpernull_Int64
}{
	ID:              whereHelperint64{field: "\"seller_shop_products\".\"id\""},
	CreatedAt:       whereHelpernull_Time{field: "\"seller_shop_products\".\"created_at\""},
	UpdatedAt:       whereHelpernull_Time{field: "\"seller_shop_products\".\"updated_at\""},
	DeletedAt:       whereHelpernull_Time{field: "\"seller_shop_products\".\"deleted_at\""},
	SellerProductID: whereHelpernull_Int64{field: "\"seller_shop_products\".\"seller_product_id\""},
	SellerShopID:    whereHelpernull_Int64{field: "\"seller_shop_products\".\"seller_shop_id\""},
}

// SellerShopProductRels is where relationship names are stored.
var SellerShopProductRels = struct {
	SellerProduct string
	SellerShop    string
}{
	SellerProduct: "SellerProduct",
	SellerShop:    "SellerShop",
}

// sellerShopProductR is where relationships are stored.
type sellerShopProductR struct {
	SellerProduct *SellerProduct `boil:"SellerProduct" json:"SellerProduct" toml:"SellerProduct" yaml:"SellerProduct"`
	SellerShop    *SellerShop    `boil:"SellerShop" json:"SellerShop" toml:"SellerShop" yaml:"SellerShop"`
}

// NewStruct creates a new relationship struct
func (*sellerShopProductR) NewStruct() *sellerShopProductR {
	return &sellerShopProductR{}
}

// sellerShopProductL is where Load methods for each relationship are stored.
type sellerShopProductL struct{}

var (
	sellerShopProductAllColumns            = []string{"id", "created_at", "updated_at", "deleted_at", "seller_product_id", "seller_shop_id"}
	sellerShopProductColumnsWithoutDefault = []string{"created_at", "updated_at", "deleted_at", "seller_product_id", "seller_shop_id"}
	sellerShopProductColumnsWithDefault    = []string{"id"}
	sellerShopProductPrimaryKeyColumns     = []string{"id"}
)

type (
	// SellerShopProductSlice is an alias for a slice of pointers to SellerShopProduct.
	// This should almost always be used instead of []SellerShopProduct.
	SellerShopProductSlice []*SellerShopProduct
	// SellerShopProductHook is the signature for custom SellerShopProduct hook methods
	SellerShopProductHook func(context.Context, boil.ContextExecutor, *SellerShopProduct) error

	sellerShopProductQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	sellerShopProductType                 = reflect.TypeOf(&SellerShopProduct{})
	sellerShopProductMapping              = queries.MakeStructMapping(sellerShopProductType)
	sellerShopProductPrimaryKeyMapping, _ = queries.BindMapping(sellerShopProductType, sellerShopProductMapping, sellerShopProductPrimaryKeyColumns)
	sellerShopProductInsertCacheMut       sync.RWMutex
	sellerShopProductInsertCache          = make(map[string]insertCache)
	sellerShopProductUpdateCacheMut       sync.RWMutex
	sellerShopProductUpdateCache          = make(map[string]updateCache)
	sellerShopProductUpsertCacheMut       sync.RWMutex
	sellerShopProductUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var sellerShopProductBeforeInsertHooks []SellerShopProductHook
var sellerShopProductBeforeUpdateHooks []SellerShopProductHook
var sellerShopProductBeforeDeleteHooks []SellerShopProductHook
var sellerShopProductBeforeUpsertHooks []SellerShopProductHook

var sellerShopProductAfterInsertHooks []SellerShopProductHook
var sellerShopProductAfterSelectHooks []SellerShopProductHook
var sellerShopProductAfterUpdateHooks []SellerShopProductHook
var sellerShopProductAfterDeleteHooks []SellerShopProductHook
var sellerShopProductAfterUpsertHooks []SellerShopProductHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SellerShopProduct) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SellerShopProduct) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SellerShopProduct) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SellerShopProduct) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SellerShopProduct) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SellerShopProduct) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SellerShopProduct) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SellerShopProduct) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SellerShopProduct) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range sellerShopProductAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSellerShopProductHook registers your hook function for all future operations.
func AddSellerShopProductHook(hookPoint boil.HookPoint, sellerShopProductHook SellerShopProductHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		sellerShopProductBeforeInsertHooks = append(sellerShopProductBeforeInsertHooks, sellerShopProductHook)
	case boil.BeforeUpdateHook:
		sellerShopProductBeforeUpdateHooks = append(sellerShopProductBeforeUpdateHooks, sellerShopProductHook)
	case boil.BeforeDeleteHook:
		sellerShopProductBeforeDeleteHooks = append(sellerShopProductBeforeDeleteHooks, sellerShopProductHook)
	case boil.BeforeUpsertHook:
		sellerShopProductBeforeUpsertHooks = append(sellerShopProductBeforeUpsertHooks, sellerShopProductHook)
	case boil.AfterInsertHook:
		sellerShopProductAfterInsertHooks = append(sellerShopProductAfterInsertHooks, sellerShopProductHook)
	case boil.AfterSelectHook:
		sellerShopProductAfterSelectHooks = append(sellerShopProductAfterSelectHooks, sellerShopProductHook)
	case boil.AfterUpdateHook:
		sellerShopProductAfterUpdateHooks = append(sellerShopProductAfterUpdateHooks, sellerShopProductHook)
	case boil.AfterDeleteHook:
		sellerShopProductAfterDeleteHooks = append(sellerShopProductAfterDeleteHooks, sellerShopProductHook)
	case boil.AfterUpsertHook:
		sellerShopProductAfterUpsertHooks = append(sellerShopProductAfterUpsertHooks, sellerShopProductHook)
	}
}

// One returns a single sellerShopProduct record from the query.
func (q sellerShopProductQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SellerShopProduct, error) {
	o := &SellerShopProduct{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for seller_shop_products")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all SellerShopProduct records from the query.
func (q sellerShopProductQuery) All(ctx context.Context, exec boil.ContextExecutor) (SellerShopProductSlice, error) {
	var o []*SellerShopProduct

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SellerShopProduct slice")
	}

	if len(sellerShopProductAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all SellerShopProduct records in the query.
func (q sellerShopProductQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count seller_shop_products rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q sellerShopProductQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if seller_shop_products exists")
	}

	return count > 0, nil
}

// SellerProduct pointed to by the foreign key.
func (o *SellerShopProduct) SellerProduct(mods ...qm.QueryMod) sellerProductQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SellerProductID),
	}

	queryMods = append(queryMods, mods...)

	query := SellerProducts(queryMods...)
	queries.SetFrom(query.Query, "\"seller_products\"")

	return query
}

// SellerShop pointed to by the foreign key.
func (o *SellerShopProduct) SellerShop(mods ...qm.QueryMod) sellerShopQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SellerShopID),
	}

	queryMods = append(queryMods, mods...)

	query := SellerShops(queryMods...)
	queries.SetFrom(query.Query, "\"seller_shops\"")

	return query
}

// LoadSellerProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (sellerShopProductL) LoadSellerProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSellerShopProduct interface{}, mods queries.Applicator) error {
	var slice []*SellerShopProduct
	var object *SellerShopProduct

	if singular {
		object = maybeSellerShopProduct.(*SellerShopProduct)
	} else {
		slice = *maybeSellerShopProduct.(*[]*SellerShopProduct)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &sellerShopProductR{}
		}
		if !queries.IsNil(object.SellerProductID) {
			args = append(args, object.SellerProductID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &sellerShopProductR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.SellerProductID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.SellerProductID) {
				args = append(args, obj.SellerProductID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`seller_products`),
		qm.WhereIn(`seller_products.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SellerProduct")
	}

	var resultSlice []*SellerProduct
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SellerProduct")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for seller_products")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for seller_products")
	}

	if len(sellerShopProductAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.SellerProduct = foreign
		if foreign.R == nil {
			foreign.R = &sellerProductR{}
		}
		foreign.R.SellerShopProducts = append(foreign.R.SellerShopProducts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.SellerProductID, foreign.ID) {
				local.R.SellerProduct = foreign
				if foreign.R == nil {
					foreign.R = &sellerProductR{}
				}
				foreign.R.SellerShopProducts = append(foreign.R.SellerShopProducts, local)
				break
			}
		}
	}

	return nil
}

// LoadSellerShop allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (sellerShopProductL) LoadSellerShop(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSellerShopProduct interface{}, mods queries.Applicator) error {
	var slice []*SellerShopProduct
	var object *SellerShopProduct

	if singular {
		object = maybeSellerShopProduct.(*SellerShopProduct)
	} else {
		slice = *maybeSellerShopProduct.(*[]*SellerShopProduct)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &sellerShopProductR{}
		}
		if !queries.IsNil(object.SellerShopID) {
			args = append(args, object.SellerShopID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &sellerShopProductR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.SellerShopID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.SellerShopID) {
				args = append(args, obj.SellerShopID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`seller_shops`),
		qm.WhereIn(`seller_shops.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load SellerShop")
	}

	var resultSlice []*SellerShop
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice SellerShop")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for seller_shops")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for seller_shops")
	}

	if len(sellerShopProductAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.SellerShop = foreign
		if foreign.R == nil {
			foreign.R = &sellerShopR{}
		}
		foreign.R.SellerShopProducts = append(foreign.R.SellerShopProducts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.SellerShopID, foreign.ID) {
				local.R.SellerShop = foreign
				if foreign.R == nil {
					foreign.R = &sellerShopR{}
				}
				foreign.R.SellerShopProducts = append(foreign.R.SellerShopProducts, local)
				break
			}
		}
	}

	return nil
}

// SetSellerProduct of the sellerShopProduct to the related item.
// Sets o.R.SellerProduct to related.
// Adds o to related.R.SellerShopProducts.
func (o *SellerShopProduct) SetSellerProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *SellerProduct) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"seller_shop_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"seller_product_id"}),
		strmangle.WhereClause("\"", "\"", 2, sellerShopProductPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.SellerProductID, related.ID)
	if o.R == nil {
		o.R = &sellerShopProductR{
			SellerProduct: related,
		}
	} else {
		o.R.SellerProduct = related
	}

	if related.R == nil {
		related.R = &sellerProductR{
			SellerShopProducts: SellerShopProductSlice{o},
		}
	} else {
		related.R.SellerShopProducts = append(related.R.SellerShopProducts, o)
	}

	return nil
}

// RemoveSellerProduct relationship.
// Sets o.R.SellerProduct to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *SellerShopProduct) RemoveSellerProduct(ctx context.Context, exec boil.ContextExecutor, related *SellerProduct) error {
	var err error

	queries.SetScanner(&o.SellerProductID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("seller_product_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.SellerProduct = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.SellerShopProducts {
		if queries.Equal(o.SellerProductID, ri.SellerProductID) {
			continue
		}

		ln := len(related.R.SellerShopProducts)
		if ln > 1 && i < ln-1 {
			related.R.SellerShopProducts[i] = related.R.SellerShopProducts[ln-1]
		}
		related.R.SellerShopProducts = related.R.SellerShopProducts[:ln-1]
		break
	}
	return nil
}

// SetSellerShop of the sellerShopProduct to the related item.
// Sets o.R.SellerShop to related.
// Adds o to related.R.SellerShopProducts.
func (o *SellerShopProduct) SetSellerShop(ctx context.Context, exec boil.ContextExecutor, insert bool, related *SellerShop) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"seller_shop_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"seller_shop_id"}),
		strmangle.WhereClause("\"", "\"", 2, sellerShopProductPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.SellerShopID, related.ID)
	if o.R == nil {
		o.R = &sellerShopProductR{
			SellerShop: related,
		}
	} else {
		o.R.SellerShop = related
	}

	if related.R == nil {
		related.R = &sellerShopR{
			SellerShopProducts: SellerShopProductSlice{o},
		}
	} else {
		related.R.SellerShopProducts = append(related.R.SellerShopProducts, o)
	}

	return nil
}

// RemoveSellerShop relationship.
// Sets o.R.SellerShop to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *SellerShopProduct) RemoveSellerShop(ctx context.Context, exec boil.ContextExecutor, related *SellerShop) error {
	var err error

	queries.SetScanner(&o.SellerShopID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("seller_shop_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.SellerShop = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.SellerShopProducts {
		if queries.Equal(o.SellerShopID, ri.SellerShopID) {
			continue
		}

		ln := len(related.R.SellerShopProducts)
		if ln > 1 && i < ln-1 {
			related.R.SellerShopProducts[i] = related.R.SellerShopProducts[ln-1]
		}
		related.R.SellerShopProducts = related.R.SellerShopProducts[:ln-1]
		break
	}
	return nil
}

// SellerShopProducts retrieves all the records using an executor.
func SellerShopProducts(mods ...qm.QueryMod) sellerShopProductQuery {
	mods = append(mods, qm.From("\"seller_shop_products\""))
	return sellerShopProductQuery{NewQuery(mods...)}
}

// FindSellerShopProduct retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSellerShopProduct(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*SellerShopProduct, error) {
	sellerShopProductObj := &SellerShopProduct{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"seller_shop_products\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, sellerShopProductObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from seller_shop_products")
	}

	if err = sellerShopProductObj.doAfterSelectHooks(ctx, exec); err != nil {
		return sellerShopProductObj, err
	}

	return sellerShopProductObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SellerShopProduct) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no seller_shop_products provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(sellerShopProductColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	sellerShopProductInsertCacheMut.RLock()
	cache, cached := sellerShopProductInsertCache[key]
	sellerShopProductInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			sellerShopProductAllColumns,
			sellerShopProductColumnsWithDefault,
			sellerShopProductColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(sellerShopProductType, sellerShopProductMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(sellerShopProductType, sellerShopProductMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"seller_shop_products\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"seller_shop_products\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into seller_shop_products")
	}

	if !cached {
		sellerShopProductInsertCacheMut.Lock()
		sellerShopProductInsertCache[key] = cache
		sellerShopProductInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the SellerShopProduct.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SellerShopProduct) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	sellerShopProductUpdateCacheMut.RLock()
	cache, cached := sellerShopProductUpdateCache[key]
	sellerShopProductUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			sellerShopProductAllColumns,
			sellerShopProductPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update seller_shop_products, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"seller_shop_products\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, sellerShopProductPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(sellerShopProductType, sellerShopProductMapping, append(wl, sellerShopProductPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update seller_shop_products row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for seller_shop_products")
	}

	if !cached {
		sellerShopProductUpdateCacheMut.Lock()
		sellerShopProductUpdateCache[key] = cache
		sellerShopProductUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q sellerShopProductQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for seller_shop_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for seller_shop_products")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SellerShopProductSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sellerShopProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"seller_shop_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, sellerShopProductPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in sellerShopProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all sellerShopProduct")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SellerShopProduct) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no seller_shop_products provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(sellerShopProductColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	sellerShopProductUpsertCacheMut.RLock()
	cache, cached := sellerShopProductUpsertCache[key]
	sellerShopProductUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			sellerShopProductAllColumns,
			sellerShopProductColumnsWithDefault,
			sellerShopProductColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			sellerShopProductAllColumns,
			sellerShopProductPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert seller_shop_products, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(sellerShopProductPrimaryKeyColumns))
			copy(conflict, sellerShopProductPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"seller_shop_products\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(sellerShopProductType, sellerShopProductMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(sellerShopProductType, sellerShopProductMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert seller_shop_products")
	}

	if !cached {
		sellerShopProductUpsertCacheMut.Lock()
		sellerShopProductUpsertCache[key] = cache
		sellerShopProductUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single SellerShopProduct record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SellerShopProduct) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SellerShopProduct provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), sellerShopProductPrimaryKeyMapping)
	sql := "DELETE FROM \"seller_shop_products\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from seller_shop_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for seller_shop_products")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q sellerShopProductQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no sellerShopProductQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from seller_shop_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for seller_shop_products")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SellerShopProductSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(sellerShopProductBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sellerShopProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"seller_shop_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sellerShopProductPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from sellerShopProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for seller_shop_products")
	}

	if len(sellerShopProductAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SellerShopProduct) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSellerShopProduct(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SellerShopProductSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SellerShopProductSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sellerShopProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"seller_shop_products\".* FROM \"seller_shop_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sellerShopProductPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SellerShopProductSlice")
	}

	*o = slice

	return nil
}

// SellerShopProductExists checks if the SellerShopProduct row exists.
func SellerShopProductExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"seller_shop_products\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if seller_shop_products exists")
	}

	return exists, nil
}
